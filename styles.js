import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    marginTop: 50,
    justifyContent: 'center',
    },
    button: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'baseline',
        justifyContent: 'center',
        alignSelf: 'baseline'
      },
  });